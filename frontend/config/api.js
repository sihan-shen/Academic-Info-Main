/**
 * API 配置
 * 开发时请将 BASE_URL 改为你的后端地址
 * - 本地: http://localhost:8000 或 http://127.0.0.1:8000
 * - 局域网: http://192.168.x.x:8000（需在微信开发者工具勾选「不校验合法域名」）
 * 上线前需在 开发 -> 开发管理 -> 开发设置 -> 服务器域名 中配置 request 合法域名
 */
module.exports = {
  // 后端 API 基础地址（不含末尾斜杠）
  // 本地开发使用 localhost:8000，需要同时启动后端服务
  // 在微信开发者工具中：设置 -> 项目设置 -> 不校验合法域名（开发环境勾选）
  BASE_URL: 'http://localhost:8000',
  // 是否启用调试（打印请求日志）
  DEBUG: true
};
